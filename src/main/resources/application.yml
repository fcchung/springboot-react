server:
  port: 9192
spring:
  datasource:
    username: root
    password: ${DATABASE_PASSWORD}
    url: ${DATABASE_URL}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      format_sql: true